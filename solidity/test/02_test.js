// const Verifier = artifacts.require('Verifier');

// const FundsVerifier = artifacts.require('FundsVerifier');
// const TwitterVerifier = artifacts.require('TwitterVerifier');

contract('Verifier', async accounts => {
    // it('Set Funds In Other Wallet ZK Proof', async () => {
    //     const verifier = await Verifier.deployed();

    //     const trx = await verifier.addProof(
    //         "0x66756e64735f6f6e5f6f746865725f636861696e730000000000000000000000",
    //         FundsVerifier.address,
    //         3,
    //         { from: accounts[1] }
    //     );

    //     console.log(trx.tx);
    // });

    // it('Set Twitter ZK Proof', async () => {
    //     const verifier = await Verifier.deployed();

    //     const trx = await verifier.addProof(
    //         "0x7477697474657200000000000000000000000000000000000000000000000000",
    //         TwitterVerifier.address,
    //         2,
    //         { from: accounts[1] }
    //     );

    //     console.log(trx.tx);
    // });
});