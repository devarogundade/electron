// const TwitterVerifier = artifacts.require('TwitterVerifier');

module.exports = async function (deployer, network, accounts) {
    return;
    // await deployer.deploy(TwitterVerifier, { from: accounts[1] });
};